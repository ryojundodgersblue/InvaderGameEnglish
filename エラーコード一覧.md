# エラーコード一覧

エラー発生時、画面・APIレスポンス・サーバーログには必ず**エラーコード**が表示されます。
利用者(Mukaさん・生徒さん)からは「コードと状況」を伝えてもらうだけで、開発側で発生箇所を特定できます。

- APIレスポンス形式: `{ "ok": false, "code": "XXX-NNN", "message": "..." }`
- サーバーログ形式: `[XXX-NNN] ルート名: 詳細`
- 画面表示形式: `メッセージ (XXX-NNN)`

## AUTH — 認証・認可

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| AUTH-001 | ID/パスワードが一致しない | IDとパスワードを再確認 | usersデータのpassword列、bcrypt照合 |
| AUTH-002 | 未ログイン or トークン無効 | 再度ログインする | Cookie(authToken)、JWT_SECRETの一致 |
| AUTH-003 | 権限がない(他人の操作/管理者専用) | 自分のアカウントで操作する | JWTのuserId/is_adminとリクエストの突合 |
| AUTH-004 | ログイン有効期限切れ(24時間) | 再度ログインする | JWT_EXPIRES_IN |

## VAL — 入力検証

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| VAL-001 | リクエストの入力値が不正 | 操作をやり直す | validation.jsのスキーマ、リクエストボディ |

## DATA — データ取得・整合性

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| DATA-001 | データの取得に失敗 | 時間を置いて再試行 | Google Sheets API疎通、SHEET_ID、認証情報 |
| DATA-002 | シートのヘッダー構成が想定と異なる | 開発者へ連絡 | シートの列構成(列の追加・削除・改名がないか) |
| DATA-003 | ローカルデータファイルがない | scripts/build_dataset.py を実行 | backend/data/*.json の存在 |
| DATA-004 | 該当データが見つからない(part/ユーザー等) | 選択内容を確認 | parts/usersデータの該当行 |

## GAME — ゲーム進行

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| GAME-001 | スコアの保存に失敗 | スコア未保存の可能性。再プレイ | scoresシートへの書込権限・API疎通 |
| GAME-002 | 進捗の更新に失敗 | 再度クリアすれば更新される | usersシートF:H列の書込、進捗不一致ログ |
| GAME-003 | 問題データの取得に失敗 | 時間を置いて再試行 | questions/answer_patterns/partsの取得 |

## TTS — 音声合成

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| TTS-001 | 音声の合成に失敗 | 音声なしでもプレイ続行可能 | Google Cloud TTS認証・クォータ、SSML構文 |

## ADMIN — 管理機能

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| ADMIN-001 | ユーザー管理の操作に失敗 | 再試行 | usersシートの書込、管理画面の入力値 |

## SYS — システム・設定

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| SYS-001 | サーバー内部エラー(想定外) | 時間を置いて再試行 | サーバーログのスタックトレース |
| SYS-002 | サーバー設定の不備 | 開発者へ連絡 | 環境変数(SHEET_ID/JWT_SECRET等) |
| SYS-404 | 存在しないAPIへのアクセス | URLを確認 | フロントとバックのルート定義のずれ |
| SYS-429 | アクセス過多(レート制限) | 1分ほど待って再試行 | rate-limit設定、異常アクセスの有無 |
| SYS-UI | 画面描画のクラッシュ | 「最初の画面に戻る」を押す | ブラウザコンソールのcomponentStack |

## NET — 通信(フロントエンドで判定)

| コード | 意味 | 利用者向け対処 | 開発側の調査ポイント |
|---|---|---|---|
| NET-001 | サーバーに接続できない | ネット接続を確認/サーバー起動を待つ | バックエンドの起動状態、URL設定 |
| NET-002 | サーバーの応答がない(タイムアウト) | 時間を置いて再試行 | サーバーの負荷、コールドスタート |

## 実装箇所

- バックエンド定義: `backend/src/utils/errors.js`(AppError / ERROR_CODES / sendError)
- フロントエンド: `frontend/src/utils/apiClient.ts`(ApiError / formatApiError)、`frontend/src/components/ErrorBoundary.tsx`
- 新しいコードを追加する場合は、`errors.js` の ERROR_CODES と本書の両方を更新すること
