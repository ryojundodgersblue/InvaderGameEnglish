# ローカル確認手順

このブランチ(`feature/data-pipeline-and-local-mode`)の成果物を、ローカルサーバーで確認する手順です。
**Google認証情報・Redisは不要**です(ローカルデータモードで動作します)。

## 1. 起動(ターミナル2つ)

```bash
# ターミナル1: バックエンド(ポート4000)
cd backend
npm install
npm run dev:local
```

```bash
# ターミナル2: フロントエンド(ポート5173)
cd frontend
npm install
npm run dev
```

ブラウザ(Google Chrome)で http://localhost:5173 を開く。

### 補足

- `dev:local` は `DATA_SOURCE=local` で起動し、`backend/data/*.json`(修正依頼673件反映済みの完成版データ)を読み書きします
- **TTS音声だけは Google Cloud 認証が必要**です。`backend/credentials.json` を置くか `.env` に `GOOGLE_CREDENTIALS_JSON` を設定すると音声が出ます。無くてもゲームは音声なしで動作します
- バックエンドを再起動するとログインセッションが切れます(AUTH-002)。`backend/.env` に `JWT_SECRET=任意の文字列` を設定すると再起動後もセッションが維持されます
- ローカルモードでのスコア・進捗・ユーザー追加は `backend/data/*.json` に保存されます。確認後に元へ戻すには `git checkout -- backend/data/` を実行してください

## 2. テストユーザー

| user_id | パスワード | 権限 | 進捗 |
|---|---|---|---|
| 10001 | vz9iByYp | 管理者(→管理画面) | 1-1-1 |
| 10004 | Hux53402 | 一般 | 3-50-1(全パート選択可) |
| 10002 | w2CAJW6y | 一般 | 1-10-1 |

※ 初回ログイン時に平文パスワードは自動でbcryptハッシュ化されます(透過移行)。同じパスワードでログインし続けられます。

## 3. 確認観点チェックリスト

### データ反映(修正依頼673件)
- [ ] 1-4-2: 解答が「This table is strong.」等に更新されている(正解表示で確認)
- [ ] 3-31-3 / 3-31-4: 新規サブパートが選択でき、関係代名詞の問題が出る
- [ ] 3-8-2: 新規パートがイラストつきで出題される
- [ ] 1-24-1 / 1-25-1: 問題が出題され、イラストが表示される(No132)
- [ ] アポストロフィが丸まった「’」でなく直線「'」で表示される

### 不具合修正
- [ ] No141: 2年生のイラストつき問題(例: 2-34-2)で問題文とイラストが重ならない
- [ ] No134/137: ゲーム中に問題が止まった場合、約20秒で「リトライ/次の問題へ/やめる」が出る。
      逆に、回答待ちで黙って20秒考えていてもダイアログは出ない
- [ ] No136: ログインボタン押下から5秒超で「サーバー起動中」の案内が出る(本番のみ意味あり)
- [ ] No138/142: read を含む問題(1-21-1等)で現在形の発音になる(要TTS認証)
- [ ] No143: 問題音声の頭が欠けない(要TTS認証)
- [ ] No133: 結果画面の「あと N 問正解でクリア!」が正しい(例: 4問正解なら あと1問)

### 新機能
- [ ] 速さランキング: ゲームをクリア後、Rankingに「Fastest Answers」が平均秒つきで表示される
- [ ] エラーコード: バックエンドを止めてログインすると「サーバーに接続できません (NET-001)」が表示される

### 画面レイアウト(マルチディスプレイ相当)
- [ ] ウィンドウ幅を変えても(1920/1366/2560/1100で確認済み)問題文・イラスト・ボタンが重ならない

## 4. 自動検証(任意)

```bash
# データ整合性(278パート×8問・画像実在・記号チェック)
python3 scripts/verify_dataset.py

# API一括検証(バックエンド起動中に実行。全パート出題・スコア・ランキング)
node backend/scripts/verify_api.mjs
```

## 5. 本番反映(今回は実行しない)

Mukaさん確認後、本番Google Sheetsへの反映は以下で行います:

```bash
cd backend
node scripts/sync_to_sheets.js            # dry-run(差分確認のみ)
node scripts/sync_to_sheets.js --execute  # 実反映(要バックアップ)
```

- 反映前にスプレッドシートのコピー(バックアップ)を作成すること
- scoresシートに `avg_answer_time` 列(G列)が追加されます(同期スクリプトが書き込みます)
- 画像は `frontend/public/questions/`(952枚)がデプロイで反映されます

## 6. Mukaさんへの要確認事項(残り)

- 2-35-1-2「New Year's Day」の差し替えイラストが未着(受領後に反映)
- Requirement修正1件の対象が特定不能(「to不定詞を使って〜」: 3-3-1/3-3-2/3-3-3のいずれか)
- 1-24-2 のイラストが未提供(現状は画像なしで動作)

詳細: `scripts/reports/apply_report.md` / エラーコードの意味: `エラーコード一覧.md`
